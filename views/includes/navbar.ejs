<nav class="navbar navbar-expand-md navbar-light bg-white shadow-sm fixed-top mb-3 border-bottom">
    <div class="container">
        <!-- Brand Icon Only -->
        <a class="navbar-brand text-primary fs-4" href="/listings">
            <i class="fa-solid fa-compass"></i>
        </a>

        <!-- Toggler Button -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Navbar Content -->
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <!-- Left Nav Buttons -->
            <ul class="navbar-nav me-auto mb-2 mb-md-0">
                <li class="nav-item">
                    <a class="nav-link btn btn-outline-primary rounded-pill px-3 mx-1" href="/listings">Explore</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link btn btn-outline-primary rounded-pill px-3 mx-1" href="/listings/new">Listing Your Home</a>
                </li>
            </ul>

            <!-- Right Side: Search + Auth Buttons -->
            <form class="d-flex align-items-center" role="search">
                <input class="form-control rounded-pill me-2" type="search" placeholder="Search..." aria-label="Search">
                <button class="btn btn-outline-success rounded-pill me-3" type="submit">Search</button>
            </form>

            <!-- Auth Buttons -->
            <div class="d-flex">

                <% if(!currentUser) { %>
                    <a href="/login" class="btn btn-outline-secondary rounded-pill me-2">Login/SignUp</a>
                    <% } %>
                        <% if(currentUser) { %>
                            <a href="/logout" class="btn btn-outline-secondary rounded-pill me-2">Logout</a>
                            <% } %>
            </div>
        </div>
    </div>
</nav>